import { Box } from '@mui/material';
import styles from './styles';

const Login = () => {
  // states

  return (
    <Box sx={[styles.root]}>
      <div>Home page</div>
    </Box>
  );
};

export default Login;
