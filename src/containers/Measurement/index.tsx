import { Box } from '@mui/material';
import styles from './styles';

const Measurement = () => {
  // states

  return (
    <Box sx={[styles.root]}>
      <div>Measurement</div>
    </Box>
  );
};

export default Measurement;