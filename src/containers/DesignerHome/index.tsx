import { Box } from '@mui/material';
import styles from './styles';

const DesignerHome = () => {
  return (
    <Box sx={styles.root}>
      <div>Hey there</div>
    </Box>
  );
};

export default DesignerHome;
